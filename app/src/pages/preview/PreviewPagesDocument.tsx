/**
 * Preview "document" is assembled at runtime: one block per PDF page, see
 * `usePreviewPdfPageBlocks` and `usePageComposition`. The PDF bytes come
 * from `ResumePdfProvider`, generated client-side from the workspace.
 */
export {};
